<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>UAMfoods</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <%= link_to '<i class="fa fa-cutlery"></i> UAMfoods'.html_safe, '/', class: 'navbar-brand' %>
        </div>
        <div class="collapse navbar-collapse">
          <% if current_admin %>
            <ul class="nav navbar-nav navbar-right">
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= "Hola, #{current_admin.name}" %> <i class="fa fa-caret-down"></i></a>
                <ul class="dropdown-menu">
                  <li><%= link_to ('<i class="fa fa-sign-out"></i> Salir').html_safe, destroy_admin_session_path, method: :delete %>
                </ul>
              <li>
            </ul>
          <% end %>
        </div>
      </div>
    </nav>

    <div class="container">
      <% if alert %>
        <div class="alert alert-danger alert-dismissible" role="alert">
          <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <b class="fa fa-warning"></b> <%= alert %>
        </div>
      <% end %>
      <% if notice %>
        <div class="alert alert-info alert-dismissible" role="alert">
          <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <b class="fa fa-info-circle"></b> <%= notice %>
        </div>
      <% end %>
      <div class="row">
        <div class="col-md-8">
          <%= yield :main %>
        </div>

        <div class="col-md-4">
          <%= yield :sidebar %>
        </div>
      </div>

      <%= yield %>

      <footer>
        <p>Made by Elias Hernandis in 2017. This project is open source under the MIT License.</p>
      </footer>
    </div>
  </body>
</html>
